# Dummy Workflow Notes

- Feature branches target `sandbox` first.
- `sandbox` is promoted into `master` through a separate PR.
- Before a new feature branch is created, `sandbox` should be refreshed from `master`.
